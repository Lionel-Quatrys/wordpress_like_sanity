export const singletonTypes = new Set(["siteSettings", "themeSettings", "homepage", "blogPage", "notFound", "header", "footer", "moduleSettings"]);

export const singletonActions = new Set([
  "publish",
  "discardChanges",
  "restore",
]);
