export const singletonTypes = new Set(["siteSettings", "themeSettings", "homepage", "blogPage", "notFound", "header", "footer"]);

export const singletonActions = new Set([
  "publish",
  "discardChanges",
  "restore",
]);
